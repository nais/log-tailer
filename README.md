# Extremely simple application to tail a single log file

Builds a statically linked tail binary from https://github.com/uutils/coreutils and executes it with the file name specified in container command.

